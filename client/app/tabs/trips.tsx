import React from "react";
import { Text, View } from "react-native";

const Trips = () => {
	return (
		<View className="  flex-1  justify-center items-center">
			<Text className=" font-bold text-3xl ">Trips</Text>
		</View>
	);
};

export default Trips;
